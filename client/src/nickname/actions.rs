//! What happens after a valid nickname passes validation (later: WS / navigation).

pub fn submit_nickname(_nickname: &str) {
    // TODO: Replace with WS request to connector when backend contract is ready.
}
